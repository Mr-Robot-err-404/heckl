[x] Render GitHub markdown in PR description
[ ] Description page — reviewers assigned
[ ] Description page — checks status
[ ] Description page — ready to merge state
[x] Video embeds — investigated, not feasible. GitHub resolves video short-links (github.com/user-attachments/assets/<uuid>) to a signed JWT URL server-side while rendering its own PR page; neither a PAT/OAuth Bearer token nor a manually-copied user_session cookie can trigger that resolution via direct HTTP request. Reverted to plain clickable links (opens on github.com, where the real logged-in session handles it). Would require scraping GitHub's rendered PR page HTML to go further — not worth the fragility for a personal tool.
[ ] PR file tree sidebar in detail view
[ ] @pierre/diffs theme matching Evergarden summer
[ ] Pagination — GitHub only returns 100 PRs, handle larger repos
[ ] Keyboard navigation — j/k through PR list, Enter to open, Escape to back
[ ] AI review sessions — walkthrough, correctness, logic, bullshit detector
